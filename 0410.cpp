/*
O(1) // custo

a ^= b // xor


1s = O(10^8)


int log(int n) {
    int ct=-1;
    while (n>0) {
        n /= 2;
        ct++;
    }
}


pair<int, int> par;
par = {2,3};
par.make_pair(2,3);

par.first;
par.second;


#define

for (set<int>::interator it = s.begin; it != s.end(); it++) {

}

s.find(i); // pointer
!= s.end();

s.lower_bound(3);

s.erase(3);

next(it);
prev(it);

*/