#include <bits/stdc++.h>
using namespace std;

const int maxn = 1e5 + 5;
int pref[maxn];


const int maxn r 1e3 + 5;
int sum[maxn][maxn];

# define int long long
const int maxn = 
cosnt int inf = 1e18 +2;

int sum[maxn][maxn]
int t[maxn]
int n, p;

bool 

int32_t main() {

    /* Forest Queries

    int n; cin >> n;
    int x; cin >> x;
    int ans = 0;

    map<int, int> mp;

    mp[0] = 1;
    for (int i = 1; i <= n;  i++){
        int a; cin >> a;
        pref[i] = pref[i-i] + a;
        ans += mp[pref[i]-x];
        mp[pref[i]] = 1;
    }

    cout << ans << '\n';

    */
    /*

    int n, q; cin >> n >> q;

    for (int i = 1; i <= n; i++l {

        for (int j = 1; j <= n; j++l {

            char a; cin >> a;
            sum[i][j] = sum[i-1][j] + sum[i-1][j] + sum[i-1][j-1] + (a == '*');

        }

    }

    for (int i = 1; i <= n; i++l {

        int x1, x2, y1, y2; cin
        cout << sum(x2 y2) - sum(x1-1 y1) - sum(x1 y1-1) + sum(x1-1 y1-1) << '\n';

    }

    */

    //n / 2 ^ i = log2 n 

    /*

    int ini = 1, fim = 100, mid;

    while (int <= fim) {

        mid = (int + fim ) /2;
        cout << mid << endl;
        int a; cin >> a;
        if (a == 0) cout << mid << endl; break;
        else if (a == 1) fim = mid - 1;
        else ini = mid + 1;

    }

    */

    /*

    int ini = 0, fim = inf, mid, best = inf;

    int n, p; cin >> n >> p; for cin t

    

    */
    
}